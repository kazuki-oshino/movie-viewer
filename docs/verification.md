# 検証記録

## 対象

2026-08-31に作成したShiori 0.1.0。
macOS 26.5.1（25F80）/ Apple Silicon、Rust 1.95、Node.js 26.4で検証。
個人の動画や参考アプリの保存領域は使わず、自作デモと一時フォルダで確認した。

## 自動検証

| 検証                                                    | 結果                                                   |
| ------------------------------------------------------- | ------------------------------------------------------ |
| TypeScript / Viteの本番ビルド                           | 成功                                                   |
| Vitest                                                  | 87件成功（画面・表示用ドメイン・保存キュー・診断情報） |
| Rustコア                                                | 22件成功                                               |
| Tauriの開発版データ分離                                 | 1件成功                                                |
| `cargo clippy --workspace --all-targets -- -D warnings` | 成功                                                   |
| Prettier / `cargo fmt`                                  | 成功                                                   |
| `git diff --check`                                      | 成功                                                   |

合計110件のテスト。
初回実装時には未追跡のテキストファイル51件も`git diff --no-index --check /dev/null <file>`で確認し、空白エラーなし。
しおりの再試行時の重複防止、更新競合、古いセッションの排除、再指定時の内容照合、破損した記録、サムネイル検証、保存失敗時の入力保持・遷移中止・終了待ちを含む。
先頭フレーム読み込み前のシークと、復元前の0秒の保存を防ぐ回帰テストも追加した。

## ブラウザで確認したこと

- 初回は空のライブラリで、明示した操作でだけデモを追加。
- しおりの時刻へ移動し、フレームの準備後に実際の動画が再生される。
- 10秒・30秒・60秒の前後操作と、先頭・末尾での範囲制限。
- `video.playbackRate`が0.1と2.0に更新される。
- 1:00の実フレームから640×360のJPEGを取得し、メモと一緒に保存。
- 追加したメモの検索、色の絞り込み、グリッド／リストの切り替え。
- 1280×720の画面で、映像と再生ボタン・速度選択を同時表示。
  調整後の再生領域は`scrollHeight === clientHeight === 534`、`scrollTop === 0`。
- ドキュメント全体の横幅は`scrollWidth === clientWidth === 1280`。

[しおり棚](screenshots/shelf-preview.png)と[プレイヤー](screenshots/player-preview.png)に画面を保存。

## ネイティブでの確認

Shiori Devを別ID・専用の一時保存先で起動した。
動画は自作の90秒H.264 / AAC MP4をコピーして使用。

- 日本語・空白・`#`・`%`を含むパスから登録・表示・再生。
- 約40.767秒のフレームを取得し、メモ・アンバー色・640×360 JPEGを保存。
- 速度メニューの0.1と2.0を操作し、保存JSONでも値を確認。
- 50.767秒・2.0倍速で`⌘ Q`により終了。次の起動でしおり棚と保存位置・速度を復元。
- しおりを押すと約40.767秒へ移動し、再生ボタンが一時停止表示に変化、時刻が進行。
- アプリ終了中に検証動画を移動・改名して再起動。サムネイルとメモは残り、元ファイルの不在を案内。
- 別内容の動画を再指定すると拒否。操作前後のJSONのSHA-256が完全一致。
- 再指定ダイアログのキャンセルでもJSONのSHA-256が完全一致。
- 正しいファイルへの再指定では、動画ID・しおりID・メモ・時刻・色・速度を維持してパスを更新。

### 初期シークの実機回帰確認

最初の実機検証では、再起動・再指定の直後に保存時刻へシークすると、時刻が進む一方で映像が黒くなるケースを検出。
先頭フレームを表示してから再度しおりへ移動すると復帰した。
先頭フレームの読み込みを明示的な0秒シークで開始し、保存位置への移動を`loadeddata`後へ変更した。
修正版を通常版のリリースビルドでも検証。再起動後、40.767秒のしおりへ直接移動して映像付きで再生し、55.433秒の実フレーム・メモ・ブルー色を追加保存できた。
この追加分も640×360のJPEGであることを確認した。
同じ通常版ビルドで動画を再度移動・再指定し、2件のしおり・55.433秒の保存位置・2.0倍速を維持したまま、停止中のフレームも正しく表示できた。
通常版の確認も一時保存先を明示し、個人利用のライブラリは読み書きしていない。

[通常版の実画面](screenshots/native-player.png)を保存した。

## コントロールのコンパクト化

同日、再生コントロールの縦幅を縮め、映像の表示領域を広げた。
再生処理と保存形式は変更していない。

1280×720のブラウザで、同じ動画を停止した状態で比較した。

| 対象                         | 調整前                                  | 調整後                        |
| ---------------------------- | --------------------------------------- | ----------------------------- |
| 映像表示領域の高さ           | 239px                                   | 372.5px（約56%増）            |
| コントロールと保存状態の領域 | 245px                                   | 111.5px（約54%減）            |
| 再生・補助操作の行数         | 2行                                     | 1行                           |
| 再生領域のスクロール         | `clientHeight === scrollHeight === 534` | 同値、`scrollTop === 0`を維持 |
| ページの横幅                 | `clientWidth === scrollWidth === 1280`  | 同値を維持                    |

- シーク・音量・速度・全ボタンの操作領域をDOMで実測し、全て40×40px以上、重なりなし。
- WebKitの検証専用アプリでも標準幅の1行配置と、最小サイズ付近の2行配置を目視確認。動画と全操作を同時表示。
- 小さいウィンドウで10秒戻る操作を行い、55.4秒から45.4秒へ移動。しおりからの再生と時刻の進行も確認。
- 操作バー内の音量・ミュート・全画面、時刻横の保存状態とエラー詳細・再試行を検証する画面テストを2件追加。
- `npm run check`、`npm run format:check`、`git diff --check`が成功。
- 通常版を`tauri build --no-bundle`と`tauri bundle --bundles app`で更新し、アドホック署名を検証。

[更新後のブラウザ表示](screenshots/player-preview.png)、[実アプリの標準幅](screenshots/native-player-compact.png)、[小さいウィンドウ](screenshots/native-player-compact-small.png)を保存した。
ネイティブの表示確認には専用データを使う開発実行を利用し、通常版の配布ビルドとは区別している。

## 再生エラーの診断表示

同日、QuickTimeでは再生できるローカルMP4がShioriではエラーになるとの報告を受け、画面に診断情報を追加した。
問題の動画は受け取らず、個人の動画・ライブラリも読み書きしていない。
再生・初期シーク・保存・ファイル配信の処理は変更していない。原因の特定や修正とは分けた診断用の変更である。

### 読みやすい詳細と共有操作

`make-interfaces-feel-better`の文字・操作領域の指針を適用した。

| Before                                                     | After                                                                                                                                                                                                                                                     |
| ---------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 再生エラーが共通メッセージだけで、動画要素の詳細が失われる | `MediaError.code`と名前、元のメッセージ、読み込み状態、時刻・長さ・映像サイズ・再生状態、URLの方式だけ、登録時サイズ、初期位置の復元状態をエラー発生時に取得して表示。詳細文が空、またはMediaErrorがない場合も明示。                                      |
| 失敗直前の読み込み段階が分からない                         | 最初のイベントからの経過ミリ秒と直近16イベントをメモリ上だけで保持。後続イベントで画面のエラースナップショットは変えず、動画切り替えでリセット。                                                                                                          |
| 共有方法がスクリーンショットや手入力になる                 | 読み取り専用の選択可能な詳細欄と42px高のコピーボタンを追加。コピー拒否・未対応時は詳細を全選択し、手動コピーを案内。既知のファイル名・パス・URLは元メッセージから伏せ、URLエンコード・Unicode表記差も検証。動画タイトル・メモ・画像は診断情報に含めない。 |
| 固定中央配置では詳細が増えると上端・下端へ届かない         | エラー欄内の縦スクロール、長文の折り返し、等幅の詳細欄を追加。短い内容の中央配置は維持し、エラー画面の白いボタンは暗い文字色へ変更。                                                                                                                      |

- 診断情報の単体テスト20件と画面テスト4件を追加。全64件のVitestと23件のRustテストが成功。
- 正常な初期シーク・保存位置の保護、既存のしおり・スキップ・速度の回帰テストも成功。
- ブラウザ検証では、配布対象外の一時ページと動画ではないテキストBlobを使って実際のメディアエラーを発生させた。`MEDIA_ERR_SRC_NOT_SUPPORTED`とブラウザ由来の詳細文を表示し、「詳細をコピー」の成功通知を確認。
- 1280×720でコピーボタンがエラー欄のスクロール前に見え、操作領域が約118×42pxであることを確認。ページの横幅は`clientWidth === scrollWidth === 1280`。エラー欄の長い内容は欄内でスクロールできる。
- 通常版を`npm run app:build`で再ビルド。実際に報告された動画でのWebKitエラー内容と再生可否は、ユーザーからの診断結果待ち。

[診断表示のブラウザ検証画面](screenshots/player-error-diagnostics.png)を保存した。

## 診断v2：配信ヘッダーとRange応答

Safariでも再生できるという報告を受け、ユーザーの承認後に追加診断と通常版の再ビルドを実施した。
問題の動画そのものは受け取っておらず、原因特定・再生修正が完了したという意味ではない。

### 取得中の案内と、安全に共有できる詳細

`make-interfaces-feel-better`の指針に沿い、既存の操作領域・長文の折り返しを保ち、取得中の状態とコピー可能になるタイミングを揃えた。

| Before                                                 | After                                                                                                                                                                                                         |
| ------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 診断v1は動画要素の状態のみ                             | `mediaDiagnostics.ts`の識別子をv2に更新。`assetDiagnostics.ts`で同じローカルasset URLのHTTPステータス、Content-Type、Content-Length、Content-Range、Accept-Rangesを取得し、独立したHEAD確認であることを明示。 |
| 配信情報を取得する処理がない                           | HEADのみ3件、先頭・末尾は各2バイトのRange指定。本文取得・GETフォールバック・外部送信は行わず、CSPの接続先だけをローカルassetに限定して追加。標準配信処理とファイルスコープは維持。                            |
| エラー表示後にすぐコピーできる                         | `MediaErrorDetails.tsx`で取得中の案内と`aria-busy`を表示し、コピーは取得完了後に有効化。全体で最大5秒とし、診断失敗時も元のエラーをコピー可能。                                                               |
| 同じセッションの再エラーでスナップショットが置き換わる | `Player.tsx`で最初のエラーを保持し、追加診断は一度だけ実行。動画切り替えで要求を中断し、遅れて届く結果を次の動画に混ぜない。                                                                                  |
| 追加ヘッダーや通信例外の非公開化は対象外               | 選択したヘッダーだけを非公開化して表示。例外文・任意の例外名・応答URL・Content-Dispositionを除外。CORSで見えないヘッダーを「不存在」と区別して表示。                                                          |
| v1のテストと操作説明                                   | 単体テスト20件・画面テスト3件を追加し、READMEと設計資料に診断手順・安全上の制約・Tauri更新時の再検証を追記。                                                                                                  |

### 実行結果

- Vitest 87件、Rust 23件、TypeScript / Vite、Clippy、変更ファイルのPrettier確認が成功。
- タイムアウト時にfetchが中断を無視しても5秒で結果を返すこと、途中キャンセル、遅れて届く例外、HTTP 403 / 404 / 416 / 500、HEADに200が返る場合、外部URLの拒否、パスの非公開化を自動検証。
- 同じmacOSの独立したWKWebViewでも、HEADとHEAD + Rangeに対する200 / 206、およびCORS経由のヘッダー取得を確認。これは標準Tauriそのものの実機検証とは区別した補助確認。
- `npm run app:build`で生成した通常版を、`open --env SHIORI_DATA_DIR=<専用の一時保存先>`で起動。通常の個人ライブラリは読み書きしていない。
- 日本語・空白・`#`・`%`を含む自作の75バイトの非動画MP4を登録し、実際のWebKitエラー4と診断v2を表示。
- 通常HEADは200、Content-Typeは`video/mp4`、Content-Lengthは75。先頭は206 / `bytes 0-1/75`、末尾は206 / `bytes 73-74/75`、両RangeのContent-Lengthは2。3件とも約36msで完了。
- Accept-RangesはCORSで参照できないため`not exposed or absent`と表示。コピー対象の診断文にはファイル名・URL・保存パスが含まれないことを確認。
- 実アプリで「詳細をコピー」の成功通知を確認。続けて正常な自作90秒動画へ切り替え、診断表示が消え、実フレームと再生時刻の進行を確認した。
- `codesign --verify --deep --strict`が成功。問題の動画での追加診断はユーザーによる確認待ち。

[診断v2の実アプリ画面](screenshots/native-player-error-diagnostics-v2.png)を保存した。

## 生成物

`npm run app:build`と`npm run app:build:dev`でアドホック署名付き`.app`を生成。
通常版は`target/release/bundle/macos/Shiori.app`、約6.8 MiB、arm64。
`codesign --verify --deep --strict`が成功し、Bundle IDは`local.kazuki.shiori`、最低OS設定は12.3。

## 2026-09-05：再生画面の余白と集中モード

### 動画領域と操作の配置

| Before                                                   | After                                                                                                                                                                                       |
| -------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 長い動画名がサイドバーの列幅を押し広げる                 | 列幅を`minmax(0, 1fr)`に制限し、ボタンとテキストの最小幅を0に設定。タイトル全文のツールチップを維持し、現在の動画には`aria-current`を追加。サイドバーは高さが足りない場合にスクロール可能。 |
| 再生中も検索・追加のヘッダーと動画名のヘッダーを両方表示 | 再生画面は56px高のヘッダー1段に集約。動画名は1行で省略し、戻る・情報・集中モードの操作を配置。                                                                                              |
| 動画の周囲の余白が大きく、右パネルが285px幅              | 動画領域の余白を基本12px／14px／10pxに縮小し、右パネルを基本264pxに調整。映像は縦横比を維持して暗い背景内に収める。                                                                         |
| 操作バーの段数がウィンドウ全体の幅で決まる               | 動画領域の幅が700px以上なら1行、それ未満は2行。コンテナクエリ未対応のWebKitでは2行にフォールバック。                                                                                        |
| 左右のパネルが常に表示される                             | 集中モードで両側を非表示にし、動画要素と再生セッションを維持。`T`で切り替え、`Esc`で解除。集中モード中のしおり追加ボタンと`B`を維持。ヘルプ・READMEに操作を追記。                           |

### 検証結果

- ブラウザの1280×720で、動画ステージの上端は214pxから108px、領域は725×372.5pxから770×490.5pxへ拡大。集中モードの領域は1252×490.5px。映像自体は縦横比に応じてこの領域内に収まる。
- 最小ウィンドウ相当の960×680と標準相当の1360×900で、長い日本語と英数字を組み合わせた表示名を検証。サイドバー・動画名のヘッダー・操作バーに横方向のはみ出しがないことを寸法と画面で確認。
- `npm test`は88件中76件成功。追加した集中モードのテストは、再生要素・時刻・再生状態の維持、しおり保存、入力中のショートカット抑止、`T`／`Esc`での復帰、棚へ戻った際のナビゲーションと検索を確認。
- 失敗した12件は診断v2を前提とした既存テスト。変更前の`HEAD`を専用の一時ディレクトリに展開して実行し、87件中75件成功・同じ12件失敗を確認。診断v4の実装は今回変更していない。
- `npm run build`と変更ファイルのPrettier検証、`git diff --check`は成功。全体の`npm run format:check`は未変更の`mediaDiagnostics.ts`と`MediaErrorDetails.tsx`、`cargo fmt --all -- --check`は未変更の`src-tauri/src/lib.rs`の既存差分で失敗。
- `Shiori Dev.app`をビルドし、`SHIORI_DATA_DIR`を外した環境と専用の`SHIORI_DEV_DATA_DIR`で起動。`public/demo/demo-0.mp4`だけを使用。WebKitでの実映像、再生中の集中モード切り替え、時刻の進行、しおりのフレーム取得・保存、通常表示への復帰を確認。保存先JSONでも時刻・メモ・サムネイルIDを確認。

- 通常版の`npm run app:build`も成功。通常版・開発版の両方で`codesign --verify --deep --strict`が成功。通常版はビルドのみ行い、個人のライブラリを使った起動・検証は行っていない。

## 2026-09-05：区間リピートとしおり散歩

便利さは「何度も確認する操作を減らす」、面白さは「保存した瞬間との偶然の再会」として検討した。
既存の保存位置・しおり・サムネイルを活用できる2機能を選び、保存形式や元動画に変更を加えず実装した。

| Before                                                 | After                                                                                                                                                        |
| ------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 同じ説明や動きを見るたびに巻き戻す必要がある           | 再生画面の「A–B」から始点・終点を指定して区間リピート。タイムライン上にも範囲を表示。設定欄は開閉でき、閉じている間も有効。                                  |
| 手動で別の位置へ移動するとリピートとの競合が起こり得る | 区間外へのシーク・スキップ・しおりジャンプで解除。一時停止を尊重し、動画末尾も折り返す。別の動画には区間を引き継がない。                                     |
| 棚のしおりは検索か一覧から選ぶ                         | 「しおり散歩」で絞り込み中のしおりをランダムに一枚ずつ表示。前後移動、一巡後の再シャッフル、選んだ時刻からの再生に対応。場所が不明な動画などは候補から除外。 |
| 新機能の操作案内がない                                 | ヘルプとREADMEに使い方を追加。リピートの無効な終点は設定欄内に案内し、候補0件の散歩は開始できない。                                                          |

- 新規テスト7件が成功。全体は95件中83件成功で、診断機能の既存12件の失敗は前項と同じ。
- リピートはA–Bの折り返し、一時停止、区間外のしおりへの移動、0.5秒の下限、動画末尾、再生待機中のキャンセル、設定欄の開閉、動画切り替え、再生位置の保存を検証。
- 散歩は一巡内の重複防止、前の一枚への復帰、再シャッフル直後の同じ一枚の回避、元データの維持、検索と色の絞り込み、候補1件・0件、場所が不明な動画の除外、選択時刻からの再生を検証。
- ブラウザで10〜12秒の実リピートと、散歩で選んだ1:11からの再生を確認。960×680でリピート設定の横幅とスクロール幅がともに482px、動画領域はともに510pxで、はみ出しなし。散歩の再生ボタンも画面内に表示。
- 区間設定と散歩の閲覧順はセッション内だけに保持する。リピートは再生イベント単位の制御で、フレーム単位の精度や音の継ぎ目の連続性は保証しない。
- 開発版を専用の一時保存領域で起動し、自作デモと前項の検証用メタデータだけを使用。WebKitで10〜20秒のリピート継続、集中モードとの併用、一時停止、区間外へのスキップでの解除、しおり追加・保存を確認。散歩は保存済みの0:09からの再生と、2枚を一巡する表示を確認した。保存先JSONの2件のしおり・再生位置と、元のデモ動画のGitハッシュ不変も確認。
- 通常版・開発版のアプリビルドと署名検証が成功。変更ファイルのPrettier検証と`git diff --check`が成功。全体の整形チェックの既存問題は前項のとおり。

## 対象外

- Developer ID署名、公証、App Store提出、他のMacへの配布検証。
- 全macOSバージョン・全コーデック・大容量NAS上の性能検証。
- Intel Mac、外付けドライブ取り外し中の操作、長時間再生の負荷検証。
- DRM付き動画、MKV・AVI、外部字幕、形式変換。
- 強制終了や電源断直前の未書き込みデータの保証。

## 2026-09-05：保存できるA–B区間しおり

| 変更前                                | 変更後                                                                      |
| ------------------------------------- | --------------------------------------------------------------------------- |
| しおりは地点再生のみ。A–B設定は一時的 | 地点／A–B区間を選んで保存。区間設定からAのサムネイルを取得して保存できる    |
| しおりを開くと開始時刻へ移動          | 区間しおりはAからリピート開始。地点しおりは区間内でもリピートを解除して再生 |
| メモと色だけを編集                    | 種類とBも編集可能。Aとサムネイルの対応は保持                                |

- 保存モデルに任意の `endSeconds` を追加。v1は読み込み可能で、読み込みだけでは更新しない。区間追加・変換時にv2へ昇格し、旧アプリによる終点の脱落を防止する。
- Rustのテストは25件成功。旧JSONの読み込み、再起動後の地点・区間保持、同一IDの再試行、種類変更、0.5秒の下限、不正値や動画長超過の拒否、元動画不変を検証。Clippy成功。
- フロントエンドは97件中85件成功。今回追加した区間保存・棚からの復元・同一動画の種類切り替え・編集時検証は成功。残る12件は前項でベースライン再現済みの診断v2テストとv4実装の不一致。
- 全体の整形チェックは従来の診断3ファイルで失敗。今回の変更ファイルは個別のPrettier／rustfmtと `git diff --check` で検証する。
- 隔離した開発版と自作デモで、10〜20秒の区間保存、Aのサムネイル取得、Bを12.5秒へ編集、終了・再起動後に棚から10〜12.5秒のリピートが自動開始・継続することを確認。JSONの `seconds: 10`、`endSeconds: 12.5`、v2、JPEGの保存を照合した。
- 停止状態のシークだけで黒いサムネイルが生じるWebKitのケースを再現。フレーム到着を待つ処理により、同じ未再生状態からA地点のデモ映像を取得できることを実機で確認。テストでもフレーム到着前には編集画面を開かず、取得後にミュートを戻して停止することを検証。
